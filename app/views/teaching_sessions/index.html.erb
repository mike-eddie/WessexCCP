<h1>Teaching Sessions</h1>

</br>

<table class="table table-condensed table-striped">
  <tr>
    <th><%= sortable "id", "ID" %></th>
    <th><%= sortable "title", "Title" %></th>
    <th><%= sortable "date", "Date" %></th>
    <th><%= sortable "teaching_format_id", "Teaching Type" %></th>
    <th>Actions</th>
  </tr>

<% @teaching_sessions.each do |teaching_session| %>
  <tr>
    <td><%= teaching_session.id %></td>
    <td><%= teaching_session.title %></td>
    <td><%= teaching_session.date %></td>
    <td><%= teaching_session.teaching_format[:format] %></td>
    <td>
      <%= link_to 'Show', teaching_session, class: "btn btn-nephritis btn-xs" %>
      <%= link_to 'Edit', edit_teaching_session_path(teaching_session), class: "btn btn-river btn-xs" %>
      <%= link_to 'Destroy', teaching_session, method: :delete, data: { confirm: 'Are you sure?'}, class: "btn btn-alizarin btn-xs"  %>
    </td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Teaching session', new_teaching_session_path, class: "btn btn-nephritis btn-sm" %>

<br />

<h2>Import Students</h2>
<%= form_tag import_teaching_sessions_path, multipart: true do %>
  <%= file_field_tag :file %>
  </br>
  <%= submit_tag "Import", class: "btn btn-wisteria btn-sm" %>
<% end %>
