<%= simple_form_for(@teaching_format) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :format %>
  </div>

  <br />

  <div class="form-actions">
    <%= f.button :submit, :class => "btn-primary" %>
  </div>
<% end %>
